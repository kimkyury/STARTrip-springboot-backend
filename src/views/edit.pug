extends base.pug

block content
    h4 Change Title of menu
    form(method="POST")
        input(name="title", placeholder="Menu Title", value=menu.title, required)
        input(value="Save",type="submit")